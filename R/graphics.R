# Way to convert svg files to pdf and png
# requires Inkscape installed

# files <- list.files("./inst/rmarkdown/templates/curriculum_vitae/skeleton/images/", pattern = ".svg", full.names = T)
# 
# for (file in files) {
#   command <- paste('inkscape  --export-type=png', file, sep = " ")
#   system(command)
#   command <- paste('inkscape  --export-type=pdf', file, sep = " ")
#   system(command)
# }
