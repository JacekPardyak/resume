%\documentclass{resume} % Use the custom resume.cls style

\documentclass[$output.font_size$,$output.paper_size$]{resume}
\usepackage[left=0.75in,top=0.6in,right=0.75in,bottom=0.6in]{geometry}
\usepackage{fontspec}
\setromanfont{$output.font$}

\name{ $contact.name$$if(contact.degree)$, $contact.degree$$endif$}
\address{ $if(contact.street)$$contact.street$$endif$ \\ $if(contact.city)$$contact.city$$endif$}
\address{ $if(contact.telephone)$$contact.telephone$$endif$ \\ $if(contact.email)$$contact.email$$endif$}

\def\nameskip{\bigskip}
\def\sectionskip{\medskip}

\begin{document}
  \begin{rSection}{Summary}
  $summary$ 
  \end{rSection}

  \begin{rSection}{Education}
    $for(education)$
    \begin{eSubsection}{$education.school$}{$education.year$}{$education.title$}{}
     \end{eSubsection}
    $endfor$
  \end{rSection}
  
  
  \begin{rSection}{Experience}
    $for(experience)$
    \begin{rSubsection}{$experience.employer$}{$experience.period$}{$experience.position$}{$experience.city$}
    $for(experience.duties)$
    \item $experience.duties.duty$
    $endfor$
    \end{rSubsection}
    $endfor$
  \end{rSection}
  
  \begin{rSection}{Skills}
    \begin{lSubsection}
    $for(skills)$
    \item $skills.skill$
    $endfor$
    \end{lSubsection}
  \end{rSection}

\end{document}
